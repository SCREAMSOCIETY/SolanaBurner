modules = ["rust", "python-3.11"]

[nix]
channel = "stable-24_05"

[workflows]
runButton = "Project"

[[workflows.workflow]]
name = "Project"
mode = "parallel"
author = "agent"

[[workflows.workflow.tasks]]
task = "workflow.run"
args = "Solana Token Burner"

[[workflows.workflow]]
name = "Solana Token Burner"
author = "agent"

[workflows.workflow.metadata]
agentRequireRestartOnSave = false

[[workflows.workflow.tasks]]
task = "packager.installForAll"

[[workflows.workflow.tasks]]
task = "shell.exec"
args = "trunk serve --port 8080 --public-url / --address 0.0.0.0 --verbose"
waitForPort = 8080

[deployment]
run = ["sh", "-c", "trunk serve --port 8080 --public-url / --address 0.0.0.0 --verbose"]

[[ports]]
localPort = 8080
externalPort = 80
